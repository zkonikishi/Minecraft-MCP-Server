# 2026-09-07 dependency update

Upstream inspected: `7f188fb3c0987c95f5c504694fddb472ce33f592`.
Mineflayer remains a native 26.2 fork; the upstream tested-version list still ends at 26.1.

Selected backports:

- `a02eb23d`: retain early window contents on window ID reuse, consume the buffer, clear pending state on login/respawn.
- `f58a33ac`: suppress physics outside PLAY and reset physics on start_configuration. The upstream automatic resource-pack acceptance behavior was not included.
- `7f188fb3`: keep resource-pack UUIDs as wire strings when acknowledging packs.

Fork regression scripts exercise three window reuse/reset scenarios and two resource-pack acknowledgement scenarios.
MCP's existing 162 tests cover its team guard, metadata mapping, reconnect and multiline collection.

Paper 26.2-92 at 127.0.0.1:29565 was started after the MCP client. The client recovered, returned player state, and received a multiline response. No team TypeError or metadata PartialReadError was observed in this run. Paper exited through console stop with code 0.

The command `/zapptest status` in this run is supplied by the synthetic MCPGate plugin. This verifies multiline transport only, not the original ZAppearance implementation. Proxy transfer, real resource-pack downloads, and the full production plugin stack were not exercised.

Minecraft data, protocol, chunk and physics dependencies remain pinned pending separate native-26.2 compatibility validation. Major-version migrations (TypeScript, Zod, AVA and ESLint) are excluded.

## Security remediation follow-up

Updated vulnerable transitive packages within their supported dependency ranges. Scoped UUID overrides for MSAL and Yggdrasil use 11.1.1, which supports CommonJS and fixes GHSA-w5hq-g745-h8pq. Both consumers use the compatible v4 API. Added tests for MSAL GUID generation, Yggdrasil client-token generation with a stubbed authentication transport, and rejection of undersized v5 output buffers. These do not constitute a real Microsoft account login test.

Pinned minecraft-protocol to the previously verified commit `7be0679a16aeab604dd38982e317bf3b1228e6dd` through an override, preventing audit/install from advancing the floating pc26_2 dependency branch.

The npm audit after remediation reports zero known vulnerabilities, including development dependencies. This is the registry advisory result as of 2026-09-07, not a claim of absence of undiscovered vulnerabilities.

Post-remediation validation: build and lint passed; all 165 tests passed. Paper 26.2-92 native login, recovery after initial connection refusal, player-state retrieval, and synthetic MCPGate multiline response passed again. No team/metadata parser errors were detected. The client, harness and Paper exited naturally; test port 29565 was released. Production port 25565 was not used.

The subsequent separately validated major dependency migration is documented in [dependency-migration-2026-09-07.md](dependency-migration-2026-09-07.md).
